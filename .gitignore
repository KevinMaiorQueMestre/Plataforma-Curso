# Segurança de Variáveis de Ambiente
.env

# Pastas Temporárias da Arquitetura DOE
.tmp/

# Logs e Cache
logs/
*.log
.DS_Store

# Dependências (Node.js / Python)
node_modules/
__pycache__/
venv/

# Build e Cache do Next.js (CRÍTICO: não rastrear - gera arquivos de até 1.7 GB)
.next/
/out/
*.tsbuildinfo
next-env.d.ts

# Pipeline ENEM — Imagens geradas (arquivos pesados, não versionar)
pipeline/processado/**/*.png

# Pipeline ENEM — Dependências Node legadas
pipeline/legado/node_modules/
